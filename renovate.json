{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigestsToSemver"],
  "minimumReleaseAge": "72 hours",
  "packageRules": [
    {
      "description": "Move every github/codeql-action/* step in a single PR. init, analyze, autobuild and upload-sarif must run the same version — analyze refuses to load a config file written by a different init.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["github/codeql-action**"],
      "groupName": "codeql-action",
      "minimumReleaseAge": "72 hours"
    }
  ]
}
