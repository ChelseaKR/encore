# Babel extraction map (docs/I18N.md G2-lite). Paths are relative to the root
# passed to `pybabel extract` — `scripts/i18n-check.sh` passes `src`, so the
# scan is the package and nothing else (no tests, no .venv).
[python: encore/**.py]
